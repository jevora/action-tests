name: Hello world

on:
  push:
    branches: ["master"]

jobs:
  hello:

    runs-on: ubuntu-latest

    steps:
      - name: Say Hello world
        run: echo "Hello world"

      - name: Check docker is installed
        run: docker --version
